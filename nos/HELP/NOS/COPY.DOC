
Topic: NOS/COPY

COPY - Copy a single file between
mount points, or to a new file on
the same mount point, or a printer.

Usage:

NCOPY|COPY
[N[n]:][path/]FILE,[N[n]:][path/]FILE

Examples:

COPY N1:file,N2:file
COPY N1:file,N1:file_new
COPY N1:file,P1:
COPY N1:file,E:
COPY "N1:my file,N2:my file"

Notes:

A filename is required in the target.
(e.g, COPY N1:file,N2: is illegal.
Wildcards are not supported.
Beware of the use of NTRANS.
Standard devices such as P1:, E: are
allowed as targets.
Enclose with quotes if a space exists.

