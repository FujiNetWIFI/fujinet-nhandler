
Topic: NOS/RUN

RUN - Jump to a specified memory
address.

Usage:

RUN hexaddr

Where <hexaddr> is a four-character
hexadecimal address.  Leading the
address with a `$` returns an error.
Addresses lower than `$1000` require
a leading zero.

Examples:
	
RUN A000
RUN 0600

